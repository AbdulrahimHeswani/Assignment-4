<h1>Employee Scheduling System</h1>

<h2>Employees</h2>

<p><a href="/logout">Logout</a></p>

{{#if employees.length}}
    <ul>
    {{#each employees}}
        <li><a href="/employee/{{this._id}}">{{this.name}}</a></li>
    {{/each}}
    </ul>
{{else}}
    <p>No employees found.</p>
{{/if}}